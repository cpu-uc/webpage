# Estructura del proyecto CPU webpage
```
/WEBPAGE
├── backend (Laravel + Astro en `resources/views` o `public`)
└── frontend (React independiente)
```

## Backend

### Configuración del entorno Laravel


1. Instalar las dependencias de PHP con Composer
```bash
cd backend
composer install
```
